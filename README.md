# 🐍 Змейка (Snake Game)

Современная веб-версия классической игры "Змейка", разработанная на React и готовая к деплою на Vercel.

## 🎮 Особенности

- **Классический геймплей** - управляйте змейкой, собирайте еду и избегайте столкновений
- **Три уровня сложности** - Легкий, Средний и Сложный с разной скоростью игры
- **Система счета** - отслеживайте свой текущий счет и рекорд (сохраняется в LocalStorage)
- **Пауза** - поставьте игру на паузу в любой момент
- **Звуковые эффекты** - приятные звуки при поедании еды, паузе и окончании игры
- **Адаптивный дизайн** - игра отлично работает на десктопе и мобильных устройствах
- **Современный UI** - красивый интерфейс с анимациями и градиентами

## 🚀 Быстрый старт

### Установка

```bash
npm install
```

### Запуск в режиме разработки

```bash
npm run dev
```

Откройте [http://localhost:5173](http://localhost:5173) в браузере.

### Сборка для продакшена

```bash
npm run build
```

### Предпросмотр продакшен сборки

```bash
npm run preview
```

## 🎯 Управление

- **Стрелки** или **WASD** - управление направлением змейки
- **Пробел** - пауза/продолжить игру

## 📦 Технологии

- **React 18+** - UI библиотека
- **Vite** - инструмент сборки
- **CSS3** - стилизация с современными возможностями
- **Web Audio API** - звуковые эффекты
- **LocalStorage API** - сохранение рекорда

## 🌐 Деплой на Vercel

### Автоматический деплой через GitHub

1. Загрузите проект на GitHub:
   ```bash
   git init
   git add .
   git commit -m "Initial commit"
   git remote add origin <your-repo-url>
   git push -u origin main
   ```

2. Подключите репозиторий к Vercel:
   - Зайдите на [vercel.com](https://vercel.com)
   - Нажмите "New Project"
   - Импортируйте ваш GitHub репозиторий
   - Vercel автоматически определит настройки из `vercel.json`
   - Нажмите "Deploy"

### Ручной деплой через Vercel CLI

```bash
npm i -g vercel
vercel
```

## 📁 Структура проекта

```
├── src/
│   ├── components/          # React компоненты
│   │   ├── Game.jsx        # Главный компонент игры
│   │   ├── GameBoard.jsx   # Игровое поле
│   │   ├── ScoreBoard.jsx  # Панель счета
│   │   ├── GameOver.jsx    # Экран окончания игры
│   │   └── Controls.jsx    # Элементы управления
│   ├── hooks/
│   │   └── useGame.js      # Кастомный хук с игровой логикой
│   ├── utils/
│   │   ├── gameLogic.js    # Вспомогательные функции игры
│   │   └── sounds.js       # Менеджер звуковых эффектов
│   ├── App.jsx             # Главный компонент приложения
│   └── main.jsx            # Точка входа
├── vercel.json             # Конфигурация Vercel
├── vite.config.js          # Конфигурация Vite
└── package.json            # Зависимости проекта
```

## 🎨 Кастомизация

### Изменение размера сетки

Отредактируйте константу `GRID_SIZE` в `src/utils/gameLogic.js`:

```javascript
export const GRID_SIZE = 20; // Измените на нужное значение
```

### Изменение скорости игры

Отредактируйте значения в `DIFFICULTY_LEVELS` в `src/utils/gameLogic.js`:

```javascript
export const DIFFICULTY_LEVELS = {
  easy: { speed: 150, name: 'Легкий' },
  medium: { speed: 100, name: 'Средний' },
  hard: { speed: 50, name: 'Сложный' }
};
```

### Отключение звуков

Звуки можно отключить, изменив `enabled` в `src/utils/sounds.js` или добавив кнопку переключения в UI.

## 📝 Лицензия

Этот проект создан в образовательных целях. Вы можете свободно использовать и модифицировать код.

## 🤝 Вклад

Вклад в проект приветствуется! Не стесняйтесь создавать issues и pull requests.

---

Создано с ❤️ используя React и Vite
